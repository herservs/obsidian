# 운영체제(OS) 관련 임시 파일
# macOS Finder에서 자동 생성됨
.DS_Store
# macOS 관련 파일
.AppleDouble
# macOS 설정 파일
.LSOverride
# Windows 탐색기에서 생성됨
Thumbs.db
# Windows 미리보기 캐시 파일
ehthumbs.db
# Windows 폴더 설정 파일
desktop.ini
# Linux에서 임시 파일로 생성됨
*~

# IDE 및 편집기 관련 파일
# JetBrains 계열 (IntelliJ, Android Studio) 설정 폴더
.idea/
# IntelliJ 프로젝트 파일
*.iml
# IntelliJ 관련 파일
*.ipr
# IntelliJ 설정 파일
*.iws
# VS Code 관련 폴더 (필요하면 주석 해제하여 포함 가능)
.vscode/
# Atom 관련 설정 폴더
.atom/

# Eclipse 관련 파일
# Eclipse 설정 파일
.classpath
# Eclipse 프로젝트 설정파일
.project
# Eclipse 환경 설정 폴더
.settings/
# Eclipse 워크스페이스 메타데이터
.metadata/
# Eclipse 추천 데이터
.recommenders/
# Eclipse factory path 설정
.factorypath

# Java & Maven 관련 파일
# Maven wrapper 관련 폴더
.mvn/
# Maven 빌드 결과물
target/
# Maven 쉐이딩 시 생성됨
dependency-reduced-pom.xml

# Docker 관련 파일
# Docker Compose 로컬 설정
docker-compose.override.yml
# Docker 빌드 시 제외할 파일 목록
.dockerignore

# 빌드 및 캐시 파일
# Flutter 빌드 결과물
/build/
# Dart 관련 도구 및 캐시 파일
.dart_tool/
# Flutter 플러그인 캐시
.flutter-plugins
# Flutter 플러그인 의존성 캐시
.flutter-plugins-dependencies
# Dart 패키지 리스트 파일
.packages
# Pub 패키지 캐시 폴더
.pub-cache/
# Dart 패키지 관련 폴더
.pub/
# Java WAR 파일
*.war
# Java JAR 파일
*.jar
# 로그 파일
*.log
# 임시 파일
*.tmp

# 웹 관련 파일
# 플러그인 자동 생성 파일 (Git에 포함할 필요 없음)
lib/generated_plugin_registrant.dart

# 테스트 및 리포트 파일
# 테스트 및 빌드 리포트
reports/
# 테스트 프레임워크(예: TestNG) 결과물
test-output/
# Maven Surefire 테스트 리포트
surefire-reports/

# 보안 및 환경 변수 관련 파일
# 환경 변수 파일 (API 키, DB 설정 포함 가능)
.env
# 로컬 환경 변수 파일
.env.local
# 환경 변수 관련 모든 로컬 설정 파일
.env.*.local
# Firebase 설정 파일 (자동 생성됨)
firebase_options.dart
# Firebase 안드로이드 설정 파일
android/app/google-services.json
# Android 키스토어 관련 파일
android/key.properties

# Android 관련 파일
# Gradle 캐시 폴더
android/.gradle/
# Firebase 설정 파일 (Git에 올리면 안 됨)
android/app/google-services.json
# Android 키스토어 파일 (보안 유지 필요)
android/key.properties

# iOS 관련 파일
# iOS 의존성 관련 폴더
ios/Pods/
# iOS 관련 심볼릭 링크
ios/.symlinks/
# Flutter iOS 관련 설정 파일
ios/Flutter/Flutter.podspec
# iOS Flutter 프레임워크 파일
ios/Flutter/App.framework
# iOS 빌드 설정 파일
ios/Flutter/Generated.xcconfig
# 임시 빌드 폴더
ios/Flutter/ephemeral/

# 디버깅 및 난독화 관련 파일
# 심볼리케이션 파일 (Crashlytics 관련)
app.*.symbols
# 난독화 맵 파일 (역디코딩용)
app.*.map.json

# Firebase 관련 파일
# Firebase 캐시 폴더
.firebase/
# Firebase 프로젝트 설정 파일
.firebaserc
# Firebase 설정 파일
firebase.json

# target 디렉터리 및 하위 모든 파일 무시
target/

# IntelliJ IDEA 관련 설정 파일 무시
.idea/

# 예외적으로 포함해야 할 파일
!/packages/flutter_tools/test/data/dart_dependencies_test/**/.packages

# 기타
bin/
.classpath
.settings/
